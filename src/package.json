{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "pack": "electron-packager . DZdp --win --out ../myClient --arch=x64 --app-version=1.0.0 --electron-version=7.1.1"
  },
  "devDependencies": {
    "electron": "^7.2.4"
  },
  "dependencies": {
    "async-request": "^1.2.0",
    "electron-packager": "^14.0.5",
    "request": "^2.88.0"
  },
  "author": "",
  "license": "ISC"
}
